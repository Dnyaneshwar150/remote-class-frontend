import { Grid } from "@mui/material";

export default function Home () {
    return (
        <Grid
        container
        height="100vh"
        sx={{
          backgroundColor: "var(--redish-orange)",
          overflow: "hidden",
          margin: 0,
          padding: 0,
        }}
      > 
      </Grid>
    )
}